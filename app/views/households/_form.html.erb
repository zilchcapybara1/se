<%= form_with(model: household, local: true) do |form| %>
  <% if household.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(household.errors.count, "error") %> prohibited this household from being saved:</h2>

      <ul>
      <% household.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.number_field :phone %>
  </div>

  <div class="field">
    <%= form.label :log %>
    <%= form.text_area :log %>
  </div>

  <div class="field">
    <%= form.label :date_of_residency %>
    <%= form.date_select :date_of_residency %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
